// Placeholder React components — full UI in the Django server-rendered pages.
import React from 'react';
const Home = () => <div>Home page (use Django: <a href="/">/</a>)</div>;
export default Home;
